var n=2;
function square(num){
    var ans = num * num;
    return ans;
}
var square2 = square(2);
var square4 = square(4);

// 1. Everything in JS happens inside execution context 
// 2. Execution context has 2 components: Memory & Code 
// 3. JS is a synchronous single threaded programming language 
// 4. A call stack is maintained by JS in order to keep track of the order of execution of execution context 



