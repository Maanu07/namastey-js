// 1. Learned about smallest Js code (empty file)
// 2. The JS code is executed by JS engine, JS engine creates a global object whenever a program is run
// 3. The variables and functions which are at top level all belongs to the global object (window)
// 4. this === window object at top level 
// 5. Js is a loosley/weekly typed language
// 6.  
 
var x = 100;
console.log(window.x)
console.log(this.x)
console.log(x)
